'use strict'

let user = {
	userName: '',
	userPass: '',
	userEmail: '',
	userRole: '',
	// методы
	// сбросить пароль ()
	// getCourses(
			// if(admin) делай одно
			// if(student) делай другое
			// if(guest) делай третье
	// )
}

admin (user) = {
	// changeUserStatus()
	// changeUserCourses()
	// getCourses()		// перезаписать метод (получить все курсы)

}

student (user) {
	// achivements()
	// getCourses()		// перезаписать метод (получить курсы конкретного студента)
}








let news = {
	date = new Date(),
}

item (news) = {
	theme: '',
	title: '',
	subTitle: '',
	text: '',
}